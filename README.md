# Trigger-word-detection-using-LSTM
An attention mechanism is implemented using LSTM network. The trigger word will be "Activate." Every time it hears you say "activate," it will make a "chiming" sound. 
